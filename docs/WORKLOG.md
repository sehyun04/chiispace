# 치이스페 누적 작업 정리

정리 기준: 2026-09-22. 최초 뼈대부터 기능 구현, 오류 수정, 검증, 배포까지 묶은 기록이다.
최초 정리는 `c8e806b`부터 `d159d0c`까지의 80개 커밋, 코드, 검증 기록과 사용자 확인을 근거로 했다.
커밋 날짜와 실제 작업·배포 날짜가 다른 경우 구분했다. 이후 9월 12일의 실제 기능 구현은 6절에 추가한다.

## 현재 어디까지 됐나

치이스페는 여러 터미널 칸에 Claude·Codex를 띄우고 작업하는 Windows 데스크톱 앱이다.
칸·탭 배치, 한글 입력, Claude 대화 복원, 칸 사이 CLI·MCP 협업이 구현되어 있다.
최근 Codex의 스크롤 유실과 칸 오종료 수정까지 배포했고, 9월 11일 사용자가 “굿 잘된다”라고 확인했다.

마지막 배포 기록은 아래 4절을 따른다. `d5dc5c9`의 스크롤·칸 종료 수정도 유지한다.
문서만 정리한 커밋과 9월 12일의 새 기능·exe 배포는 구별한다.
9월 12일에 Codex 칸별 대화 복원을 추가했다. 상세 범위와 검증은 아래 6절에 기록한다.
9월 13일에는 설치된 Codex 0.154의 호환 수정과 재배포를 7절에 추가했다.
9월 15일에는 기존 커밋 푸시를 마쳤고 현재 핸드오프를 압축했다. 기록은 8절에 있다.
이어진 요청으로 권한 프로필의 칸별 자동 복원을 수정·검증·배포했다. 기록은 9절에 있다.
9월 16일 사용자가 입력·복원 실패를 다시 신고했고, 17일에 네이티브 이어가기 전환과 백스페이스 수정을 배포했다.
현재 복원 정책과 검증은 10절을 우선한다. 1·6·7·9절의 ID 기반 복원은 당시 구현 이력이다.
9월 22일에는 continue 정책을 유지하면서 칸의 자동 이름도 저장·복원하도록 보강했다. 상세는 11절에 있다.
실모델 자연어 협업 검증까지 끝났다는 뜻은 아니다.

## 1. 기능별로 진행한 작업

### 앱 기반과 터미널 연결

- kasaterm 엔진 위에 워크스페이스 뼈대를 만들고 Tauri 2 + React + xterm.js 구조로 전환했다.
- PTY·VT 파서는 직접 새로 만들지 않고 `kasa-pty`를 사용한다. 앱은 배치·세션·에이전트 연결을 맡는다.
- PTY 원시 바이트를 웹뷰에 전달하고, 입력·리사이즈·종료를 연결했다. 청크 경계의 UTF-8도 보존한다.
- 독립 실행 파일에 웹 자산을 포함했다. `custom-protocol` 누락으로 release exe가 Vite를 찾던 문제를 수정했다.
- 빌드에 TypeScript 검사를 포함하고, 터미널 엔진과 소켓 엔진의 버전을 같은 고정 rev로 관리한다.
- 앱 이름을 `chiispace`로 정리했다. 대화 저장 경로와의 연결을 지키기 위해 실제 폴더 이름은 `kasaspace`로 유지했다.

근거: `c8e806b`, `2b1027d`, `1c12990`, `9e94a97`, `768ab25`, `ace33a9`, `4595996`, `d5c2df8`.
주요 파일: [Cargo.toml](../src-tauri/Cargo.toml), [lib.rs](../src-tauri/src/lib.rs).

### 칸·탭·작업공간

- 좌우·상하 분할, 경계선 드래그, 방향별 포커스 이동, 칸 닫기를 구현했다.
- 헤더를 끌어 칸을 서로 바꾸거나 다른 칸의 가장자리에 붙이는 이동 기능을 구현했다.
- 칸을 늘릴 때 전체 배치를 균형 있게 펴서, 마지막 칸만 계속 작아지는 문제를 해결했다.
- 탭마다 배치·폴더·셸을 분리했다. 새 탭에서는 실제 설치된 셸을 고를 수 있다.
- 기본 셸은 PowerShell 계열을 우선하고, 없는 셸을 고정 목록으로 노출하지 않도록 했다.
- 사이드바를 세션 목록 중심으로 개편하고, 탭 이동·칸 선택·이름·캐릭터·폴더 표시를 모았다.
- 폴더 선택, git 브랜치·변경 파일 수, 사이드바 접기와 빈 탭 정리를 구현했다.

초기에 만든 파일 탐색기는 이후 세션 목록으로 교체했다. 현재 파일 탐색기가 있는 것으로 세지 않는다.
Alt 조합의 칸 이동도 철회하고 드래그 방식으로 바꿨다. 과거 커밋의 모든 기능이 현재 남아 있는 것은 아니다.

근거: `4a90921`, `792eb99`, `88fac51`, `77364ea`, `7890864`, `37c7505`, `94c1e3b`, `d3cf9e6`, `72448e3`.
주요 파일: [App.tsx](../ui/App.tsx), [layout.ts](../ui/layout.ts), [Sidebar.tsx](../ui/Sidebar.tsx), [shells.rs](../src-tauri/src/shells.rs).

### 입력·스크롤·화면 표시

- OS 클립보드 복사·붙여넣기, 글자 크기 조절, 링크 열기와 10,000줄 스크롤백을 연결했다.
- 한글 조합 중 자모 누출, 중복 확정, 한 글자에서 입력이 끊기는 문제를 순차 수정했다.
- 조합 종료 이벤트를 놓친 뒤 입력이 계속 막히는 문제를 blur·키 이벤트로 풀도록 했다.
- 칸 이동 후 포커스·복사가 먹통 되는 문제를 수정했다. 입력 대상은 현재 칸 ID로 찾는다.
- 칸 DOM을 평평하게 유지해 배치 변경이 터미널 언마운트와 PTY 종료로 이어지지 않도록 했다.
- Claude 스크롤백을 유지하고, 위로 스크롤한 동안 입력 상자를 화면 아래에 보조 표시한다.
- 밝은 테마에 맞춰 텍스트 대비를 조정하고, 터미널에 최소 대비 보정을 적용했다.
- UI 폰트·터미널 폰트와 폴백을 정리하고 사이드바 글자 크기를 조정했다.

근거: `c168aed`, `6f83ef5`, `a5b9021`, `bfa5815`, `a10b6b0`, `7c9b613`, `db6df58`, `e58ad13`, `69b019a`.
주요 파일: [Term.tsx](../ui/Term.tsx), [theme.css](../ui/theme.css), [app.css](../ui/app.css).

### 세션 저장과 Claude 대화 복원

아래 ID 기반 동작은 과거 이력이다. 현재는 10절의 네이티브 continue·중복 칸 선택 목록 정책을 쓴다.

- 탭·칸 배치·폴더·셸·글자 크기·이름·캐릭터 배정과 복원 명령을 저장한다.
- 시작 직후 아직 에이전트가 잡히지 않거나 종료 중 프로세스가 사라져도 복원 정보를 바로 지우지 않도록 했다.
- 여러 칸이 같은 Claude 대화로 몰리지 않도록 칸별 세션 ID를 저장하고 복원한다.
- 복원할 새 대화는 앱에서 고유 ID를 정한다. 아직 대화 파일이 없는 ID는 새 세션으로 연다.
- ID가 불명확한 칸은 다른 칸·백그라운드·다른 창의 대화를 무작정 가져오지 않도록 후보를 제한했다.
- 저장된 대화가 백그라운드에서 실행 중이면 `attach`로 연결한다.
- 다른 창이 전경에서 붙들고 있는 대화는 ID와 명령을 유지하되 자동 실행하지 않는다.
- 복원 시 Claude가 다시 출력하지 않는 이전 대화는 마지막 40개 주고받기를 셸보다 먼저 표시한다.
- 칸 이름과 Claude 대화 이름을 연결하고, 이름이 없을 때만 마지막 요청 등으로 대신 표시한다.
- 일반 명령은 다시 쳐 놓기만 하며 빌드·배포 등을 자동 재실행하지 않는다.

이는 앞으로의 오연결을 막는 처리다. 과거에 잘못 연결된 대화의 소유권을 근거 없이 재배정하거나,
어느 대화인지 기록이 없는 모든 칸을 복구한 것은 아니다.

근거: `90237d8`, `65506c3`, `9d9ef99`, `fd6ab50`, `89688d1`, `0f228d9`, `6f8fa95`, `244af1a`, `2b2eecf`, `11831df`.
주요 파일: [session.ts](../ui/session.ts), [workspace.rs](../src-tauri/src/workspace.rs), [App.tsx](../ui/App.tsx).

### 칸 사이 기본 연결과 에이전트 협업

- 앱마다 별도 named pipe를 만들고 `chiispace-cli.exe`를 통해 칸 목록·화면 조회를 제공한다.
- 텍스트·키 전송, 분할, 포커스 이동과 닫기를 연결했다. 기본 분할은 기존 포커스를 유지한다.
- 새로 실행하는 Claude·Codex에 실행별 stdio MCP를 붙였다. 앱 내부 실행 래퍼를 사용한다.
- `context`, `delegate`, `status`, `claim`, `complete`, `cancel`, `peek` 도구를 제공한다.
- 상대 칸에 초안이 있거나 작업 중이면 위임 요청을 대기시킨다. 빈 입력창 확인 후 작업 ID만 알린다.
- 요청 본문·결과는 MCP로 교환한다. 상대 화면만 보고 작업 완료로 단정하지 않는다.
- 칸·에이전트가 재시작하거나 종료되면 이전 실행에 묶인 미완료 작업을 정리한다.
- 터미널 자동 조회 응답을 사용자 초안으로 오인하던 문제와 named pipe 연결 경합을 보완했다.
- 전역 PATH·모델·권한·사용자 설정을 덮어써서 연결하지 않는다.

기본 CLI 연결과 협업 대기열은 구현·통합 검증됐다. Claude의 네이티브 `SendMessage`·팀 인박스 연동은 아니다.
실제 모델이 자연어 요청을 이해해 다른 모델에 맡기고 결과를 회수하는 전체 흐름은 별도 실사용 검증이 남아 있다.

근거: `2b2eecf`, `421f1a7`.
주요 파일: [bridge.rs](../src-tauri/src/bridge.rs), [CLI](../src-tauri/src/bin/chiispace-cli.rs),
[collab.rs](../src-tauri/src/collab.rs), [mcp.rs](../src-tauri/src/mcp.rs), [launchers.rs](../src-tauri/src/launchers.rs), [ui/bridge.ts](../ui/bridge.ts).

### Codex 스크롤과 칸 오종료

1. 실행 래퍼에 `--no-alt-screen`을 붙였다. 이미 지정된 옵션은 중복하지 않고 resume 인자도 유지했다.
2. 이 옵션만으로 Windows 10에서 스크롤백이 생기지 않는 것을 실제 설치된 Codex에서 재현했다.
3. Microsoft ConPTY `1.24.260710001`을 검증·내장해 영역 스크롤이 화면 덮어쓰기로 유실되는 경로를 개선했다.
4. PTY 엔진과 xterm의 중복 장치·커서·색 응답이 프롬프트에 섞이지 않도록 했다.
5. 배포 후 종료 신고 때 직전 exe로 원복했다. 이후 사용자가 앱 전체가 아니라 Codex 칸만 종료됐다고 확인했다.
6. 출력 구독이 64청크 지연으로 끊겨도 앱이 이를 셸 종료로 처리하던 버그를 재현·수정했다.
7. 이제 화면 연결만 끊기면 스냅샷과 재구독으로 복구한다. 실제 EOF와 Windows 셸 프로세스 종료는 별도 확인한다.
8. 재검증 후 고정 경로에 앱·CLI를 교체했고 사용자가 정상 동작을 확인했다.

스크롤 수정과 칸 종료 수정은 모두 현재 배포본에 들어 있다. 출력 복구는 엔진에 남은 화면·스크롤백 범위다.
모든 원시 출력, Claude 프리로드 전체, 재시작 전 Codex 대화의 자동 복원까지 제공하는 것은 아니다.
신고 당시 전체 실행을 똑같이 재현한 것은 아니며, 별도로 재현한 오종료 버그 수정과 사용자 확인을 구분해 기록한다.

근거: `4ca8e5b`, `3f3e6c4`, `428c745`, `d5dc5c9`.
주요 파일: [launch.rs](../src-tauri/src/launch.rs), [conpty.rs](../src-tauri/src/conpty.rs),
[terminal-queries.ts](../ui/terminal-queries.ts), [pty_stream.rs](../src-tauri/src/pty_stream.rs).

### 테마·캐릭터와 작업 표시

- 치이카와 밝은 테마, 칸별 캐릭터 배정과 저장, 준비된 그림만 선택하는 표시 구조를 구현했다.
- 현재 기본 캐릭터 이미지 12개와 대응하는 작업 중 이미지 12개가 있다.
- 정지 이미지에서 작업 중 움직임을 만드는 스크립트와 움직임 세기 조절을 추가했다.
- 에이전트가 열려 있다는 이유만으로 작업 표시가 켜지지 않도록 사용자 제출·현재 작업 상태로 판정을 좁혔다.
- 캐릭터 테두리와 작업공간의 시각 구조를 분리하고, 세션 목록이 중심이 되도록 배치를 조정했다.

이것은 이미 진행된 작업의 기록이다. **추가 캐릭터 보강은 최후순위이며 사용자가 요청할 때만 한다.**

근거: `d663dba`, `2fa9b86`, `990778d`, `7fe3bae`, `5677ed5`, `650b9e5`, `d4830e8`, `8b8e0ba`.
주요 파일: [roster.tsx](../ui/roster.tsx), [캐릭터 리소스](../ui/assets/faces), [make-motion.py](../scripts/make-motion.py).

## 2. 날짜별 진행 흐름

| 시기 | 주요 결과 |
|---|---|
| 8월 30일 | kasaterm 기반 워크스페이스 첫 뼈대 |
| 8월 31일 | Tauri·xterm 전환, 칸·탭·저장·클립보드·단독 exe, IME 수정 시작 |
| 9월 1~2일 | 입력·포커스 안정화, Claude 칸별 대화 ID, 대화 이름, 세션 중심 사이드바, 캐릭터 배정 |
| 9월 3~4일 | 균등 분할·빈 탭 정리·화면 개편, 엔진 갱신, 코드 분리, 셸 선택, 백그라운드 attach, 앱 이름 정리 |
| 9월 5일 | 복원 정보 유실 방지, Claude 지난 대화 표시, 입력 상자 보조 표시, 작업 판정과 대비 개선 |
| 9월 6~7일 | 복원 오연결 방지와 CLI·MCP 협업, 다른 창의 대화 보호, 고정 exe 경로, 자동 조회 응답 처리. 주요 구현 커밋은 9월 7일 |
| 9월 9~10일 | Codex 일반 스크롤 옵션, 실제 Windows 스크롤 유실 재현, 내장 ConPTY·중복 응답 수정. 종료 신고 후 일시 원복 |
| 9월 11일 | 출력 지연과 실제 셸 종료 구분, 재검증·재배포, 사용자 정상 확인. 핸드오프 현황과 과거 이력 분리 |
| 9월 12일 | 먼저 누적 작업 문서 통합(문서만 변경). 이후 Codex 칸별 대화 복원 구현·검증과 고정 경로 배포 |
| 9월 13일 | Codex 0.154의 복원 권한 전달·치이스페 간 대화 잠금·초기 연결 경합 수정과 재검증·재배포 |
| 9월 15일 | 기존 커밋 18개·메모리 문서 푸시. 후속 요청으로 권한 프로필 자동 복원 수정·검증·고정 경로 재배포 |

## 3. 검증한 범위

아래는 9월 11일 최종 코드 검증 기록이다. 9월 12일 문서 정리 중 새로 앱·모델을 실행한 결과로 표현하지 않는다.

| 검증 | 결과와 범위 |
|---|---|
| 프런트 빌드 | TypeScript + Vite 통과. 번들 크기 경고는 남음 |
| Rust | 21개 통과. 연결·대기열·입력 판정·복원·실행 인자·화면 지연/수명 처리 |
| JS 단위 | 14개 통과. 세션 선택·초안 보호·조회 응답·xterm VT/UTF-8 복구 |
| 기본 연결 | 별도 앱 두 개의 순차 기동, 칸 목록·조회·입력·분할·포커스·닫기와 앱 간 격리 |
| 협업 | 모델 대역을 실제 PTY·MCP에 연결해 대기·수락·결과·실패·취소·재시작 격리 검증 |
| 실제 Codex | 네이티브 및 평소 npm/Node 경로, `/status` 6개 누적, 휠 상단·하단 이동, 입력 오염 방지. PowerShell에서 `/quit` 코드 0과 셸 유지 |
| 독립 시작 | 사용자 배치만 복제한 별도 상태 파일에서 탭 2개·칸 2개 유지 |
| 사용자 데이터 보호 | 테스트 전후 사용자 세션·전역 설정 해시 일치. 사용자 대화·인증 미복제, 모델 요청 없음 |
| 사용자 확인 | 최종 배포 후 최근 Codex 문제에 대해 “굿 잘된다” 확인 |

검증 파일: [session](../scripts/session.test.mjs), [agent-input](../scripts/agent-input.test.mjs),
[terminal-queries](../scripts/terminal-queries.test.mjs), [pty-stream](../scripts/pty-stream.test.mjs),
[bridge](../scripts/bridge.test.mjs), [collab](../scripts/collab.test.mjs),
[codex-scroll](../scripts/codex-scroll.test.mjs), [startup](../scripts/startup.test.mjs).

일반 `npm test`는 실제 앱 검증을 환경 변수 없이 실행하지 않는다. 실행법은 [README의 헤드리스 검증](../README.md#헤드리스-검증)을 따른다.
대역 협업 통과를 실모델 자연어 위임 완료로, 순차 앱 기동 통과를 동시 기동 해결로 확대하지 않는다.

## 4. 커밋과 배포 상태

- 실행 경로 고정: `src-tauri/target/release/chiispace.exe`와 같은 폴더의 `chiispace-cli.exe`.
- 마지막 배포 코드: `58e3fe0`. 2026-09-23 00:18:09에 앱·CLI를 함께 반영했다.
  위임 알림 제목이 칸 이름을 덮지 않게 한 수정이며 상세는 12절에 있다.
  칸 자동 이름 저장·복원(`ed4fe86`), 네이티브 이어가기와 백스페이스 수정은 유지했다.
- 내부 검증 빌드와 고정 경로 파일의 SHA-256 일치를 확인했다.
  `target/agent-bridge/release/`는 내부 빌드·검증용이지 사용자 실행 위치가 아니다.
- 직전 배포본 백업: `src-tauri/target/release/backups/before-delegation-title-20260923-001617/`.
  9월 22일의 앱·CLI와 교체 시점의 `session-before.json`을 보관했다. 백업 해시를 확인한 뒤 교체했다.
  **이번에는 사용자 앱이 실행 중이었다.** 임의 종료·재시작하지 않고, 실행 중인 두 exe 를
  `*.old-20260923-001809` 로 옮겨 낸 뒤 같은 자리에 새 파일을 놓았다. 떠 있던 앱과 CLI 4개는
  그대로 살아 있고 새 코드는 다음에 앱을 켤 때부터 적용된다. 사용자 세션 원본은 수정하지 않았다.
- 배포 번들은 사용자 워킹트리가 아니라 `HEAD` + 이번 수정만 올린 임시 git 워크트리에서 구웠다.
  다른 세션이 `ui/Term.tsx` 와 `ui/powershell-render.ts` 를 작업 중이어서, 평소처럼 워킹트리를
  그대로 빌드하면 검증하지 않은 남의 변경이 사용자 앱에 섞여 들어간다.
- 이전 백업 `before-pane-titles-20260922-224021/`,
  `before-native-continue-20260917-140527/`, `before-codex-profile-20260915-223350/`와
  `before-codex-0154-20260913-204234/`도 같은 backups 폴더에 유지한다.
- 문서 기록 커밋과 실행 파일의 코드 커밋은 구분한다. 최종 푸시·작업 트리 상태는 Git을 확인한다.

| 파일 | SHA-256 |
|---|---|
| `chiispace.exe` | `5A523F0BD5224354D11DA0543EE24C31FCA26C91CA28E0300B2440A9AC22DBAF` |
| `chiispace-cli.exe` | `4B88D0910BA122E61B76852A58D913AE44E602BBA5E79095F42D85D03EEE1123` |

교체 직후 사용자 세션 SHA-256: `2CD117A405E9542C91A13C66D3048C08F70083219DDDD5C34040B32C19C72C80`.
앱이 계속 떠 있어 사용자가 칸을 쓰는 대로 이 값은 바뀐다 — 배포가 세션을 건드리지 않았다는 근거는
이 해시가 아니라, 교체한 것이 exe 두 개뿐이고 검증이 `CHIISPACE_STATE` 임시 파일만 썼다는 사실이다.
사용자가 새 앱을 열면 앱이 정상 저장 과정에서 이름을 보존한다. 테스트에서 직접 세션을 고치지 않는다.
백업 세션을 사용자 확인 없이 덮어씌워 원복하지 않는다.

## 5. 아직 남아 있는 것

| 항목 | 현재 경계 |
|---|---|
| 대화 이어가기의 범위 | 현재는 10절의 폴더별 최근 대화. 같은 폴더의 중복 칸은 선택 목록. 원래 칸 ID 자동 복원·다른 앱과의 중복 차단은 보장하지 않음 |
| Codex 외부 원격 실행 | 자체 원격 중계를 제거. 사용자가 지정한 외부 `--remote`는 로컬 최근 대화로 임의 변환하지 않음 |
| 실모델 자연어 위임 | 연결·도구·대기열은 검증했지만 실제 모델 간 자연어 위임 전체 확인은 남음 |
| 네이티브 팀 연동 | Claude 팀 인박스·SendMessage는 미구현. 현재는 실행별 MCP |
| 에이전트 자동 생성·지속 작업 | 자동 생성과 앱 재시작을 넘는 작업 예약·복원은 미구현 |
| 앱 동시 기동 | 정확히 동시에 띄웠을 때의 초기화 타임아웃은 미진단 |
| 일반 UI 확장 | 창 분리, 설정 화면, 테마 전환, 터미널 검색 미구현 |
| 이미지·크기 최적화 | 리소스 경량화 여지 있음. 추가 캐릭터 보강은 요청 시에만 진행 |

이 목록은 남은 범위이며, 사용자의 새 요청 없이 자동으로 착수할 우선순위나 실행 지시가 아니다.

## 6. 9월 12일 추가: Codex 칸별 대화 복원

구현 커밋: `86d407e`. 고정 경로 배포는 위 4절을 따른다.

- Codex TUI는 유지하고 실행별 로컬 App Server 연결에서 실제 대화 시작·복원·분기 응답을 받는다.
  파일 생성 시각·프로세스 이름·가장 최근 대화로 소유자를 추측하지 않는다.
- 칸별 대화 ID, `CODEX_HOME`, 작업 폴더와 명시적인 모델·프로필·권한 옵션을 저장한다.
  셸에 경로를 직접 이어 붙이지 않으며 프롬프트·첨부·임의 MCP 인증을 세션 파일에 복제하지 않는다.
- `/new`의 새 ID로 갱신한다. 다른 칸·다른 창·과거 실행의 토큰으로 현재 칸의 연결을 덮지 않는다.
- 첫 메시지 전에는 Codex 디스크에 대화가 없다는 사실을 실제 테스트에서 확인했다.
  빈 대화는 새 빈 대화로 열고, 첫 turn이 수락된 뒤부터 해당 ID를 이어 연다.
- 없는 ID나 다른 창이 사용하는 ID의 복원 실패는 ID와 명령을 보존하고 자동 실행을 중단한다.
  옆 칸과 앱은 계속 유지한다. ID 없는 구버전 칸은 첫 회 대화 선택 목록을 연다.
- 로컬 연결은 루프백·임의 포트·256비트 실행별 인증으로 제한하고 브라우저 Origin을 거절한다.
  생성한 서버와 자식들은 Windows Job으로 정리한다. 사용자 전역 설정·권한·인증 파일은 수정하지 않는다.
- 시작 직후 빈 프로세스 조회가 복원 명령을 지우던 코드에서 누락된 보호 조건도 복구했다.
  실제 실행을 본 칸만 연속 네 번 비었을 때 복원 정보를 놓도록 기존 회귀 방지 규칙을 적용한다.
- 실제 Codex와 로컬 가짜 Responses 서버로 두 칸별 ID·이전 본문, 재시작, 빈 대화, `/new`,
  숨겨진 탭, 다른 창 충돌, 없는 ID에서 앱·옆 칸 생존을 검증한다.
  기존 스크롤·칸 연결·MCP 협업·독립 시작 검증도 함께 유지한다.

새 구현은 치이스페 래퍼를 거친 로컬 대화형 Codex 대상이다. 절대 경로·별칭 우회와 사용자가 직접
지정한 외부 원격 서버, 비대화형 실행까지 자동 추적한다고 확장하지 않는다. 일회성 설정 중 인증을
포함할 수 있는 임의 `-c` 값도 복제하지 않는다. 공식 App Server WebSocket은 실험적 인터페이스이므로
Codex 갱신 때 실제 복원 검증을 다시 수행한다. 실모델 자연어 위임은 여전히 별도 작업이다.

최종 검증: TypeScript·Vite 빌드, Rust 30개, JS 단위 17개 통과. 앱 연결·MCP 협업·독립 시작 검증도 통과했다.
Codex `0.153.4` 네이티브와 npm/Node 양쪽에서 복원 전체 시나리오와 스크롤을 각각 재검증했다.
실행별 프로필의 모델이 복원 뒤 실제 로컬 테스트 요청에도 유지됨을 확인했다.
상태 출력 6개·휠 이동·PowerShell에서 `/quit` 코드 0과 셸 유지도 양쪽 경로에서 확인했다.
사용자 세션·전역 설정 해시는 모두 유지됐다. Vite의 번들 크기 경고는 남아 있다.

당시 주요 파일: `codex_transport.rs`(10절에서 제거, Git 이력에 보존),
[codex_session.rs](../src-tauri/src/codex_session.rs), [launch.rs](../src-tauri/src/launch.rs),
[session.ts](../ui/session.ts), [App.tsx](../ui/App.tsx), [복원 검증](../scripts/codex-resume.test.mjs).
실제 실행 파일의 반영 상태는 [현재 핸드오프](../CLAUDE.md)의 배포 기록을 기준으로 한다.

## 7. 9월 13일 추가: Codex 0.154 호환 재검증

구현 커밋: `87e14c0`. 2026-09-13 20:42:34 고정 경로 배포 완료.
직전 배포는 `86d407e`, 9월 12일 18:38:26이며 당시 백업은
`src-tauri/target/release/backups/before-codex-resume-20260912-183826/`에 남아 있다.

9월 12일 배포본을 고정 경로에서 다시 검증하니 설치된 Codex가 `0.153.4`에서 `0.154.0`으로 바뀌어 있었다.
기존 결과만으로 완료 처리하지 않고 새 버전에서 재현된 문제를 후속 수정했다.

- 원격 TUI의 복원 시 `--sandbox`·`--ask-for-approval` 거절을 확인했다. 같은 로컬 App Server의
  실행 설정과 시작·복원·분기 요청에 명시적인 값을 전달한다. 값을 삭제하거나 더 넓은 권한으로 바꾸지 않는다.
- 공식 문서의 예제뿐 아니라 설치된 Codex의 `generate-ts` 결과로 실제 enum 표기를 확인했다.
  이 버전의 sandbox·승인 값은 `read-only`·`on-request` 같은 표기이며 문서 예제의 camelCase와 다르다.
- Codex 자체의 중복 실행 제한에 의존하지 않고 상태 폴더·대화 ID별 Windows mutex를 잡는다.
  복원 요청을 보내기 전에 검사하며 실행 종료 때 해제한다. 이 잠금에 참여하지 않는 외부 Codex까지
  보호한다고 확대하지 않는다. `/new` 이전에 열었던 ID도 그 실행이 끝나기 전까지 보수적으로 보유한다.
- 비차단 리스너에서 받은 소켓을 명시적으로 차단 모드로 전환한 뒤 WebSocket 핸드셰이크를 한다.
  첫 바이트가 늦게 오는 연결을 조기에 닫지 않도록 지연 입력 회귀 테스트를 추가했다.
- 실제 turn 기록에서 읽기 전용 유지와 이번 실행의 승인 정책 적용을 확인한다.
  설정 문자열이 저장됐다는 사실만으로 권한이 적용됐다고 판단하지 않는다.

당시 제한: 0.154에서 **권한 설정을 담은 별도 `--profile`의 원격 복원은 거절됐다.**
프로필 제거·권한 변경으로 통과시키지 않고 원래 ID와 명령을 보존하는 실패 경로를 검증한다.
모델 전용 프로필은 정상 복원 대상이다. Codex가 폐기한 승인 옵션도 임의로 다른 값으로 치환하지 않는다.
내부 자동 복원에 대한 후속 해결은 9절에 있다.

9월 13일 배포 SHA-256: 앱 `8329CE8EE12508258F664CC71C0268A4EEC942225869E3277C3826014BEDF4BB`,
CLI `24266800206C5CA746251C523F9EA4556CFB3A4D2C17E5CB24BCCF0C8D1758C5`.
9월 15일 메모리 정리 때도 일치했으며, 이후 9절 작업에서 새 배포본으로 교체했다.

최종 검증: Rust 33개, JS 단위 17개 통과. 실제 Codex `0.154.0` 네이티브·npm/Node 양쪽에서
복원 전체 시나리오와 스크롤 6회 누적·휠 이동·정상 종료 후 셸 유지를 통과했다.
검증 디렉터리는 네이티브 복원 `chiispace-codex-resume-lsqFLB`, 스크롤 `chiispace-codex-scroll-ZTEpNB`,
npm 복원 `chiispace-codex-resume-qnxyUK`, 스크롤 `chiispace-codex-scroll-BRrDLz`이다.
배포된 고정 경로의 exe로 다시 실행한 npm 복원 검증도 `chiispace-codex-resume-M1yabU`에서 통과했다.
모두 Windows 임시 폴더 아래의 격리된 상태이며 최종 실행의 사용자 세션·설정 전후 해시는 일치했다.
앱 연결·대역 MCP 협업·독립 시작도 재검증했다. 실제 외부 모델 간 자연어 위임은 포함하지 않는다.

검증 중 npm 경로의 휠 상단 이동 대기 시간 초과가 한 차례 있었고 재실행에서는 통과했다.
그 현상의 원인까지 이번 복원 수정으로 해결했다고 기록하지 않는다. 한 실행에서 `.claude.json`의
전후 해시 차이도 감지됐다. 해당 원본을 쓰거나 되돌린 작업은 없으며 다른 실행의 변경을 덮어쓰지 않았다.

## 8. 9월 15일 추가: 푸시 완료와 작업 메모리 정리

- 사용자 요청으로 `master`의 미푸시 커밋 18개를 `origin/master`에 반영했다.
  원격은 `https://github.com/sehyun04/chiispace.git`, 반영 범위는 `0381e98..b5840d9`다.
  당시 미커밋 변경은 없어 새 커밋을 만들지 않았다. 푸시 후 로컬·원격 HEAD와 깨끗한 작업 트리를 확인했다.
- 이어진 메모리 정리에서 `CLAUDE.md`의 현재 요약을 압축하고 이 누적 기록에 푸시·배포 확인을 추가했다.
  기존 회귀 방지 결정과 과거 핸드오프는 삭제하지 않았다. 이 정리로 커밋·푸시를 새로 하지는 않았다.
- 코드·실행 파일·사용자 세션·설정·대화 내역은 변경하지 않았다. 실행 파일은 해시만 재확인했다.
  9월 13일 기능 검증 결과를 9월 15일에 다시 실행한 것처럼 기록하지 않는다.
- 새 자동 복원의 사용자 실사용 확인, 권한 포함 프로필 제한, 실모델 위임·동시 기동 등 남은 범위를 유지했다.
  새 기능 자동 착수 지시는 없으며 캐릭터 보강은 사용자 요청 전까지 보류한다.
- 이후 사용자가 문서 커밋·푸시와 권한 프로필 복원 제한 확인·해결을 승인했다. 이 후속 요청으로
  메모리 문서를 `02b07b1`로 커밋·푸시하고 9절 작업을 진행했다. 동시 기동·실모델 위임 후보는 착수 범위가 아니다.

## 9. 9월 15일 추가: 권한 프로필의 칸별 자동 복원

구현 커밋: `be77736`. 당시 배포는 2026-09-15 22:33:51이며 기록 커밋은 `b403f02`다.
당시 SHA-256은 앱 `4C9BB8B9A16678C3B27E1C29A2300106CE4B8E011839889AF28AAD47DA34FD34`,
CLI `DEFDFA723D6FC1D90A1E11827083F246773623FD387C1816635A380AFFCEA79A`다.
현재 배포와 이전 파일 백업은 4절을 따른다.

- 설치된 Codex는 여전히 `0.154.0`이었다. 기존 실행 파일에서 권한 프로필 복원 거절을 먼저 재현했다.
- OpenAI Docs의 [시작·복원 규약](https://learn.chatgpt.com/docs/app-server#start-or-resume-a-thread)과
  [설정 프로필 계층](https://learn.chatgpt.com/docs/config-file/config-advanced#profiles), 설치본의 생성된 스키마를 확인했다.
  프로필을 서버의 `-c`로 재조립하면 프로젝트보다 우선할 수 있어 프로필 해석을 Codex TUI에 그대로 맡긴다.
- 내부 `codex-resume`의 검증된 저장 ID에 한해서 bootstrap `resume <id>`를 분리하고,
  TUI의 첫 `thread/start`를 같은 ID의 `thread/resume`으로 전달한다. 프로필 인자·설정·권한·지침은 유지한다.
  새 대화 전용 메타데이터만 제외하며 요청 전 대화 잠금과 응답 ID 확인을 모두 거친다.
- 복원이 확인되기 전 다른 시작·분기·turn 요청을 막는다. 없는 ID·사용 중인 ID·설정 오류를 새 대화로
  대체하지 않고 원래 ID·명령을 보존한다. 성공 뒤에는 일회성 복원 처리를 해제해 `/new`가 새 ID를 받는다.
- 사용자 전역 설정·인증·대화 원문을 복제하거나 변경하지 않는다. 수동 Codex 명령, 구버전 ID 없는 선택 목록,
  외부 원격 서버에는 이 변환을 적용하지 않는다. 캐릭터 작업·실모델 위임·동시 기동 수정도 하지 않았다.

검증 결과:

- Rust 36개, JS 단위 17개, TypeScript·Vite 빌드 통과. Vite 번들 크기 경고는 기존대로 남아 있다.
- 실제 Codex 네이티브·npm/Node 경로에서 빈 대화, 칸별 이전 본문·ID, `/new`, 숨긴 탭, 사용 중인 ID,
  없는 ID, 권한 프로필, 스크롤 6회·휠 상하단·정상 종료 코드 0과 셸 유지를 통과했다.
- 프로필 복원 뒤 실제 모델 입력에 원래 칸의 대화가 있고 다른 칸의 대화가 없는지 확인했다.
  실제 turn 기록으로 `CLI > 프로젝트 > 프로필 > 기본 설정`의 모델·샌드박스·승인 정책을 검증했다.
  테스트 설정 파일의 해시와 프로필 인자도 유지됐다. 문법 오류 프로필은 ID·명령을 보존하고 자동 재시도를 멈춘다.
- 앱 연결, 대역 PTY·MCP 협업, 독립 시작 검증도 통과했다. 외부 모델은 호출하지 않았다.
- 최종 네이티브 복원 `chiispace-codex-resume-ONjY6x`, 스크롤 `chiispace-codex-scroll-hXR9IL`;
  npm 복원 `chiispace-codex-resume-EBGn7t`, 스크롤 `chiispace-codex-scroll-ruUSqS`.
  배포본 npm 재검증은 복원 `chiispace-codex-resume-vPs1tj`, 스크롤 `chiispace-codex-scroll-RJ4M7A`에서 통과했다.
  모두 Windows 임시 폴더 아래이며 최종 실행의 사용자 세션·전역 설정 전후 해시는 일치했다.

검증 중 관찰도 남긴다. 초기 시험에서 기능 검증은 통과했지만 `.claude.json` 해시 차이로 전체 검증이
실패한 실행이 있었다. 별도 Claude 프로세스 3개가 실행 중이었으나 변경 주체를 확정하지 않았고 원본을 되돌리지 않았다.
또한 Codex 0.154는 존재하지 않는 프로필 파일을 오류로 처리하지 않았다. 치이스페가 이 규칙을 임의로
바꾸지 않으며, 실패 보호 검증은 실제 거절되는 문법 오류 프로필로 수행한다.
내부 테스트 앱 실행 중 재빌드가 파일 잠금으로 한 차례 실패해 테스트 종료 뒤 다시 빌드했다.
이는 사용자 앱 종료나 별도 동시 기동 문제 수정으로 처리하지 않았다.

## 10. 9월 17일 추가: 네이티브 이어가기와 백스페이스 수정

9월 16일 사용자 재신고: 백스페이스 삭제가 이상하고 Claude·Codex 모두 복원이 안 됨.
ID 복원을 더 확장하지 않고 사용자 요청대로 CLI의 continue를 쓰는 방향으로 전환했다.
구현 커밋은 `08a0c4a`, 당시 고정 경로 배포는 2026-09-17 14:05:29다.
당시 SHA-256은 앱 `B44B567803C7590127E472F2986A9800A7AF491B860ADDB7EE5379F1C846D290`,
CLI `0DA2E83F01811DE944AFC40A8299E98116E2B6DBD6DDB8E0AC73D3A3502A9593`다.
현재 배포와 이 파일들의 백업은 4절을 따른다.
이전 테스트 통과 기록은 그대로 남기되 실사용 성공으로 간주하지 않는다.

변경:

- Claude는 `claude --continue`, Codex는 `codex resume --last`를 자동 실행한다.
  구형 `--resume <id>`·`--session-id`와 Codex ID 메타데이터를 최초 한 번 네이티브 정책으로 전환한다.
  전환 뒤 실패해 자동 실행을 멈춘 명령은 다음 시작에서 임의 재시도하지 않는다.
- 같은 폴더의 같은 에이전트는 첫 칸만 이어가고 나머지는 `claude --resume`·`codex resume` 선택 목록을 연다.
  복원할 대화를 앱이 새로 만들거나 과거 파일에서 재배정하지 않는다. 원래 칸의 정확한 대화 복원이 아닌
  폴더의 최근 대화라는 제약을 안내한다. 다른 앱·외부 CLI와의 독점 연결 보장도 하지 않는다.
- Claude의 자동 ID 탐색·background attach 판정·대화 원문 프리로드를 제거했다.
  잘못 추정한 대화를 미리 보여 주지 않고 실제 CLI가 선택·표시하도록 한다. 수동 attach 등 임의 명령은 보존한다.
- Codex의 자체 App Server·원격 TUI 중계와 `tungstenite` 의존성을 제거했다.
  `codex_transport.rs`는 Git 이력에 남아 복구할 수 있다. 실행별 MCP와 스크롤 옵션은 유지한다.
  실행 정보는 상태 폴더·작업 폴더·허용된 모델·프로필·권한 옵션만 저장하며 ID·대화·인증은 넘기지 않는다.
  `-C`는 실제 폴더로 정규화해 상대 경로 중복 적용과 잘못된 폴더 중복 판정을 막는다.
- 백스페이스는 늦게 등록된 textarea 리스너가 아닌 xterm 커스텀 키 핸들러에서 IME 상태를 먼저 판정한다.
  조합 종료 유실 뒤 첫 삭제 키가 버려지는 현상을 기존 빌드에서 재현했다.
  조합 중 삭제는 브라우저에 맡기고, blur 뒤 xterm이 낡은 조합을 다시 보내는 경로도 차단한다.
  한글 확정 직접 전송·중복 확정 차단은 유지한다.
- 시작 직후 복원 명령 보존, ConPTY 고정 버전, 중복 조회 응답 차단, 구독 지연 시 화면 재연결은 유지한다.
  캐릭터 보강·실모델 위임·동시 기동 수정은 작업하지 않았다.

검증:

- TypeScript·Vite 빌드, Rust 28개, JS 단위 13개 통과. 일반 `npm test`는 실제 앱 테스트 7개를 건너뛴다.
  제거한 자체 중계 테스트 대신 네이티브 이어가기 정책과 실제 CLI 검증으로 교체했다.
- 실제 WebView 키 이벤트와 PTY 수신 바이트로 일반 삭제, 조합 종료 유실 뒤 첫 Backspace,
  조합 중 삭제의 브라우저 처리, blur 뒤 유령 글자 차단, 한글 확정 1회 전송을 검증했다.
  물리 IME의 모든 입력 조합이나 사용자 실사용 확인을 대신하지는 않는다.
- 실제 Codex와 로컬 가짜 Responses 서버로 작업 폴더별 최근 대화·이전 본문, `/new`,
  숨긴 탭, 중복 칸 선택 목록, 프로필·읽기 전용·승인 정책 유지와 설정 오류 후 앱 생존을 확인한다.
- 실제 Claude는 별도 상태 폴더의 합성 대화가 `--continue`로 표시되는지 확인한다.
  테스트용 폴더·키만 승인하며 API 연결 확인 `HEAD /api/hello`는 루프백 테스트 서버에서 받는다.
  모델 요청은 보내지 않고 사용자 대화·인증을 가져오지 않는다.
- 초기 Claude 테스트는 Windows 경로 표기가 다른 테스트 신뢰 설정과 초기 선택 시점 때문에 실패했다.
  테스트 프로젝트의 두 경로 표기를 저장하고 선택 상태가 화면에 반영된 뒤 확인하도록 테스트를 수정했다.
  중간 한 실행에서는 사용자 `.claude.json` 해시 차이도 검출됐다. 별도 Claude 프로세스들이 있었으나
  변경 주체는 확정하지 않았고 원본을 되돌리거나 덮어쓰지 않았다. 최종 실행에서는 전후 해시를 다시 확인한다.
- Vite 번들 크기 경고와 공유 Rust 모듈의 CLI 전용 메서드 dead-code 경고는 남아 있다.

최종 검증은 설치된 Codex `0.154.0`·Claude Code `2.1.273`을 사용했다.
내부 빌드의 실제 앱 검증 7개를 순차 실행해 모두 통과했다.
네이티브 Codex 복원 `chiispace-codex-resume-ZoLaDB`, 스크롤 `chiispace-codex-scroll-O2nEOq`;
Claude `chiispace-claude-continue-Q1ABM6`, 입력 `chiispace-input-CVZ7DM`;
칸 연결 `chiispace-bridge-wOWQxh`, 협업 `chiispace-collab-soxT1w`, 독립 시작 `chiispace-startup-NGSxQG`.
npm 경로 복원 `chiispace-codex-resume-q2u2DT`와 스크롤 `chiispace-codex-scroll-mX5Rr1`도 통과했다.
npm 복원에는 상대 `-C`로 연 작업 폴더 보존도 포함하며 스크롤은 출력 6개·77줄·휠 상하단·종료 코드 0과 셸 유지를 확인했다.
모두 Windows 임시 폴더 아래의 자체 테스트 상태다. 사용자 세션·설정 해시는 최종 테스트에서 일치했다.

고정 경로 배포 뒤에도 실제 앱 검증 4개를 다시 실행해 모두 통과했다.
Claude `chiispace-claude-continue-sAH2ZN`, Codex npm 복원 `chiispace-codex-resume-IRKPaC`,
스크롤 `chiispace-codex-scroll-bvZjac`, 키 입력 `chiispace-input-SgfMcV`다.
배포 후 테스트도 별도 상태를 사용했고 사용자 세션·설정 해시가 일치했다.

## 11. 9월 22일 추가: 칸 자동 이름 저장과 복원

사용자 요청: 다시 열었을 때 pane 이름이 없으면 어느 칸인지 구분할 수 없으므로 이름도 복원할 것.
기존 exe에서 수동 `names`만 보존되고 자동 이름은 사라지는 현상을 별도 세션으로 재현했다.
구현 커밋은 `ed4fe86`이며 고정 경로 배포·해시·백업은 4절과 같다.

- 자동 터미널 제목을 `paneTitles`에 별도로 저장하고 시작 시 복원한다. `names`와 구분해 수동 이름이 항상 우선한다.
  새 CLI 제목이 오면 자동 이름을 갱신하고, 수동 이름을 지우면 마지막 자동 이름으로 돌아간다.
- 헤더·사이드바·칸 연결 도구에 같은 이름을 제공한다. 숨긴 탭도 보존하고 닫힌 칸은 저장할 때 제외한다.
  제목 안의 `/`를 경로로 오인해 자르지 않고 상태 기호·제어 문자를 정리하며 길이를 제한한다.
- 셸 실행 경로, CLI의 기본 프로그램 이름, cmd가 이전 제목 뒤에 잠깐 붙이는 실행 명령은 보존 대상에서 뺀다.
  실제 PTY 테스트에서 `이전 이름 - title Codex`가 저장 이름을 오염시키는 것을 발견해 함께 차단했다.
- 대화 이어가기 정책·ID 연결·PTY·키 입력·캐릭터는 바꾸지 않았다. Rust 소스 변경도 없다.
  이름은 마지막 칸 표시일 뿐 continue가 원래 대화를 선택했다는 증거가 아니다.
  과거 버전이 저장하지 않은 자동 이름을 새로 추측하지 않으며 CLI가 제목을 다시 보낼 때부터 보존한다.

TypeScript·Vite 빌드와 JS 단위 16개를 통과했다. 일반 `npm test`는 실제 앱 테스트 8개를 건너뛴다.
실제 PTY의 제목 출력으로 헤더·사이드바·연결 도구 일치, 저장 후 재시작, 숨긴 탭,
수동 이름 우선·해제, 기본 제목 덮어쓰기 방지, 닫힌 칸 정리를 확인했다.
검증 폴더는 Windows 임시 폴더의 `chiispace-pane-titles-XROl69`이며 사용자 세션 해시는 유지됐다.
기존 Vite 번들 크기 경고와 공유 Rust 모듈의 CLI 전용 메서드 dead-code 경고는 그대로다.

기존 실제 앱 검증 7개도 순차 실행해 모두 통과했다. 설치된 Codex `0.154.0`의 npm 경로와
Claude Code `2.1.278`을 사용했고 외부 모델·사용자 인증·대화는 사용하지 않았다.
복원 `chiispace-codex-resume-xYHyBH`, 스크롤 `chiispace-codex-scroll-mKUyom`,
Claude `chiispace-claude-continue-OP1Gfw`, 연결 `chiispace-bridge-4UAYnL`,
협업 `chiispace-collab-CAPdql`, 독립 시작 `chiispace-startup-1ef5Ey`, 입력 `chiispace-input-OWKJ8g`다.
배포 뒤 고정 경로의 이름 검증도 `chiispace-pane-titles-dGsuIn`에서 통과했다.
사용자 세션·설정 해시는 유지됐다. 사용자 앱은 처음에는 실행 중이었으나 배포 전에 닫힌 것을 확인했고,
임의 종료·재시작하지 않았다. 캐릭터 보강도 하지 않았다.

## 12. 9월 23일 추가: 위임 알림이 칸 이름을 덮던 문제

사용자가 칸 사이 위임(`chiispace_delegate`)을 실제로 써 보다가 발견했다. 위임을 받은 칸의
이름이 `Chiispace task-42488-6` 으로 바뀌어 눌러앉았다.

### 무엇이 원인이었나

`collab.rs` 의 `collab_deliver` 는 받는 칸에 다음 문구를 입력으로 넣는다.

```
Chiispace task-42488-6: use chiispace_claim, then chiispace_complete.
```

이것이 그 칸 에이전트에게는 **첫 프롬프트**다. Claude Code 는 첫 프롬프트로 대화 이름을 짓고
그 이름을 터미널 제목으로 내보내므로, 앱에는 여느 자동 이름과 똑같은 모양으로 도착한다.
앱이 알림을 제목으로 삼은 것이 아니라 에이전트가 알림을 제 대화 이름으로 삼은 것이고,
그 둘은 제목 문자열만 보아서는 구별되지 않는다.

사용자가 claude 의 `/rename` 으로 붙인 이름도 같은 자리(`paneTitles`)에 있다. 헤더에서 직접
붙이는 이름(`names`)만 자동 이름보다 앞서므로, `/rename` 만 쓰는 사용자에게는 쪽지 한 통이
이름을 밀어내는 것으로 보인다. 실제 세션 파일에서도 살아 있는 세 칸이 모두 `paneTitles` 에만
있었고 `names` 에는 이미 닫힌 옛 칸 둘만 남아 있었다.

### 무엇을 고쳤나

`ui/session.ts` 의 `paneTitle` 은 이미 "작업 이름이 아닌 것"(셸 전체 경로, `claude --continue`
같은 실행 명령)을 걸러 내는 자리다. 여기에 위임 알림에서 비롯한 이름을 함께 거른다.
`savedPaneTitles` 가 같은 필터를 타므로 이미 저장된 이름도 다음 저장에서 사라진다.

### 검증

- `npm test` 30개 중 21개 통과·0 실패(나머지 9개는 실제 앱 검증이라 건너뛴다), `tsc --noEmit` 통과.
- 새 단위 테스트는 알림에서 나온 이름 5종을 거르고 "치이스페 칸 이름 수정", "chiispace 배포 준비"
  같이 사람이 붙인 이름은 남기는지 확인한다.
- 필터 두 줄을 일부러 빼고 돌려 새 테스트가 실제로 실패하는 것을 확인한 뒤 되돌렸다.
  통과만 보고 회귀 테스트가 문다고 단정하지 않는다.
- 실제 앱 검증(`scripts/pane-titles.test.mjs`)에 위임 알림 제목을 넣었다. 실제 PTY 에
  `title Chiispace task-42488-6` 을 쳐서 제목이 앱까지 도착한 것을 `data-title` 로 확인하고,
  헤더·옆 목록·`surface.list`·저장 파일의 이름이 그대로인지 본다. 배포할 바이너리로 통과했다
  (`chiispace-pane-titles-GtyUve`). 사용자 세션 파일 두 개의 SHA-256 은 실행 전후로 같았다.

### 남은 경계

- 에이전트가 알림을 제 말로 요약해 제목을 지으면 이 필터에 걸리지 않는다. 관찰된 것은 원문을
  거의 그대로 쓰는 경우였다. 확실히 막으려면 `collab_deliver` 가 입력을 넣은 시점을 기억해
  그 직후의 첫 제목 변경을 자동 이름으로 받지 않아야 하며, 이는 별도 작업이다.
- `/rename` 으로 붙인 이름은 여전히 자동 이름 자리에 있어 다른 제목에 밀릴 수 있다.
  그 이름만 `names` 급으로 승격하는 것도 별도 작업이다.
- 칸 이름 문제와 별개로 위임 자체는 양방향으로 동작한다. 같은 날 두 칸 사이에서 위임·수락·
  결과 회수를 서로 한 번씩 주고받아 확인했다.

## 문서별 역할

- 이 문서: 지금까지 무엇을 만들고 고쳤으며 어디까지 검증했는지 보는 누적 작업 정리.
- [README](../README.md): 현재 기능·사용법·구조·빌드·검증 실행법.
- [CLAUDE.md](../CLAUDE.md): 다음 작업자가 먼저 읽는 현재 상태·안전 규칙·회귀 방지 결정.
- [핸드오프 이력](HANDOFF-ARCHIVE.md): 당시 진단·시행착오·원복 기록의 원문 보관. 현재 작업 지시는 아님.
