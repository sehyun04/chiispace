# 앱은 이 파일의 바이트를 exe 에 박고, 캐시에 이미 풀린 것과 한 바이트라도 다르면 시작을
# 거부한다(src-tauri/src/conpty.rs 의 install). core.autocrlf 가 체크아웃 때 CRLF 로 바꾸면
# 그 판으로 구운 앱은 켜자마자 "ConPTY cache mismatch" 로 죽는다 — 배포용 임시 워크트리에서
# 실제로 그렇게 구워졌다. 어느 체크아웃에서 구워도 같은 바이트가 되게 변환을 끈다.
src-tauri/CONPTY-LICENSE.txt -text
